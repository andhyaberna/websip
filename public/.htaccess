<IfModule mod_rewrite.c>
 RewriteEngine On

 # jika file/folder ada, jangan rewrite
 RewriteCond %{REQUEST_FILENAME} -f [OR]
 RewriteCond %{REQUEST_FILENAME} -d
 RewriteRule ^ - [L]

 # rewrite semua ke index.php
 RewriteRule ^ index.php [L]
</IfModule>